{{#with model as |reminder|}}

  <h2 class="reminder-title">{{reminder.title}}</h2>
  <h5 class="reminder-date">{{reminder.date}}</h5>
  <p class="reminder-notes">{{reminder.notes}}</p>

  {{#link-to 'reminders.edit' reminder}}
    Edit
  {{/link-to}}
{{/with}}

{{outlet}}
